#!/bin/bash
# ЗАГЛУШКА — используется при тестировании run.sh
# Предотвращает выполнение основного скрипта при source
# Все переменные уже установлены run.sh к этому моменту
return 0 2>/dev/null || true
